<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.github.adrianosiqueira</groupId>
    <artifactId>hash-tools</artifactId>
    <version>3.0.0</version>

    <name>HashTools</name>
    <description>Application for checking and generating hashing checksums, and for file comparisons.</description>
    <url>https://github.com/AdrianoSiqueira/hash-tools</url>

    <developers>
        <developer>
            <name>Adriano Siqueira</name>
            <url>https://github.com/AdrianoSiqueira</url>
        </developer>
    </developers>

    <properties>
        <encoding>UTF-8</encoding>
        <java.version>11</java.version>
        <javafx.version>19.0.2.1</javafx.version>
        <junit.version>5.9.3</junit.version>
        <logback.version>1.4.8</logback.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-fxml</artifactId>
            <version>${javafx.version}</version>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>${logback.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
            </plugin>
            <plugin>
                <groupId>org.openjfx</groupId>
                <artifactId>javafx-maven-plugin</artifactId>
                <version>0.0.8</version>
                <configuration>
                    <compress>2</compress>
                    <noHeaderFiles>true</noHeaderFiles>
                    <noManPages>true</noManPages>
                    <stripDebug>true</stripDebug>
                    <mainClass>hash.tools/hashtools.main.Main</mainClass>
                    <launcher>hashtools-launcher</launcher>
                    <jlinkImageName>${project.build.finalName}</jlinkImageName>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
